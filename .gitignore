node_modules/
# NOT dist/ - web/dist/index.html is hand-written source, not build output.
# This project has no build step, and that one file is the entire front end.
# Ignoring it kept the whole UI out of the repo and off every teammate's clone.
*.sqlite
*.sqlite-journal
.atrium/
.env
JOIN.txt
*.sqlite-wal
*.sqlite-shm
*.log

# The filled-in launcher carries the room token - the join credential.
# Only the .template belongs in the repo.
Join Atrium.cmd
Join Atrium.command
